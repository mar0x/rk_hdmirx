- https://github.com/radxa-repo/bsp.git
  tool to fetch, patch and build "vendor kernel"

	hdmiin_dc: hdmiin-dc {
		compatible = "rockchip,dummy-codec";
		#sound-dai-cells = <0>;
	};

	hdmiin-sound {
		compatible = "simple-audio-card";
		simple-audio-card,format = "i2s";
		simple-audio-card,name = "rockchip,hdmiin";
		simple-audio-card,bitclock-master = <&dailink0_master>;
		simple-audio-card,frame-master = <&dailink0_master>;
		status = "okay";
		simple-audio-card,cpu {
			sound-dai = <&i2s7_8ch>;
		};
		dailink0_master: simple-audio-card,codec {
			sound-dai = <&hdmiin_dc>;
		};
	};

- https://github.com/radxa/kernel.git
  repo fetched by bsp tool

- https://github.com/armbian/linux-rockchip.git
  armbian version of "vendor kernel"

- https://github.com/rockchip-linux/kernel.git
  SoC vendor kernel

- add the line "extraargs=initcall_blacklist=hdmirx_init" into /boot/armbianEnv.txt
  to block built-in module initialisation, then rebuild and insmod fixed version

- fix 3.5mm jack microphone detection
  - make -C /lib/modules/`uname -r`/build M=$PWD modules
  - sudo armbian-add-overlay rk3588-es8316-sound.dts
  - reboot
  - sudo insmod rk_multicodecs_fixed.ko
